export abstract class DetectADOConstants {
    static readonly BLACKDUCK_ID = 'BlackDuckScaService'
    static readonly BLACKDUCK_PROXY_ID = 'BlackDuckScaProxyService'

    static readonly BLACKDUCK_API_TOKEN = 'apitoken'

    static readonly PROXY_USERNAME = 'username'
    static readonly PROXY_PASSWORD = 'password'

    static readonly DETECT_RUN_MODE = 'DetectRunMode'

    static readonly DETECT_ARGUMENTS = 'DetectArguments'

    static readonly DETECT_FOLDER = 'DetectFolder'
    static readonly DETECT_VERSION = 'DetectVersion'

    static readonly DETECT_AIR_GAP_JAR_DIRECTORY_PATH = 'DetectAirGapJarDirectoryPath'

    static readonly DETECT_RUN_MODE_SCRIPT = 'UseScript'
    static readonly DETECT_RUN_MODE_AIR_GAP = 'UseAirGap'

    static readonly ADD_TASK_SUMMARY = 'AddTaskSummary'

    static readonly SCRIPT_DETECT_FOLDER = 'detect'
}
